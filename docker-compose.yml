services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: perevozka-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./prisma:/app/prisma
